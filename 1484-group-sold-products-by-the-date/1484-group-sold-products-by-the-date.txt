# Write your MySQL query statement below
-- select sell_date , count(Distinct sell_date) as sum ,product from Activities
-- group by product

-- SELECT sell_date, product, COUNT(*) AS sum
-- FROM Activities
-- GROUP BY sell_date, product;


SELECT 
    sell_date,
    COUNT(DISTINCT product) AS num_sold,
    GROUP_CONCAT(DISTINCT product ORDER BY product SEPARATOR ',') AS products
FROM Activities
GROUP BY sell_date
ORDER BY sell_date;
