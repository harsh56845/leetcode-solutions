CREATE FUNCTION getNthHighestSalary(N INT) RETURNS INT
BEGIN
set N = N-1;
  RETURN (
      # Write your MySQL query statement below.
    --   select in(?
        -- select id , salary from Employee group by salary;
        -- ) as getNthHighestSalary from Employee id=N;
        select distinct(salary) from employee order by
        salary desc
        limit 1 offset N
  );
END