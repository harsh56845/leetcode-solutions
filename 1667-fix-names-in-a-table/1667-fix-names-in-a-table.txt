# Write your MySQL query statement belowUPD
-- UPDATE users
-- SET name = CONCAT(UPPER(SUBSTRING(name,1,1)),LOWER(SUBSTRING(name,2)));

SELECT user_id,
CONCAT(UPPER(SUBSTRING(name,1,1)),LOWER(SUBSTRING(name,2))) as name
 FROM users
 ORDER BY user_id;

